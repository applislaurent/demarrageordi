import { FormulaireCreationDto } from './formulaire-creation-dto';

describe('FormulaireCreationDto', () => {
  it('should create an instance', () => {
    expect(new FormulaireCreationDto()).toBeTruthy();
  });
});
